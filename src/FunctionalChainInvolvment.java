public class FunctionalChainInvolvment extends ChainElt {
    // Attributes
    // id, name and summary are inherited from ChainElt class

    // Constructor

    /**
     * @param id      the id to set
     * @param name    the name to set
     * @param summary the summary to set
     */
    public FunctionalChainInvolvment(String id, String name, String summary) {
        super(id, name, summary);
    }

    // Getters
    // All getters are inherited from ChainElt class for id, name and summary


    @Override
    public String toString() {
        return super.toString();
    }
}
